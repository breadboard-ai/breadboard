module.exports = {
  // Your other configuration, such as extends, parser, etc.
  // For example:
  // extends: ['eslint:recommended'],
  // parser: '@babel/eslint-parser',

  rules: {
    // Other rules you have configured...

    // Disable the 'no-constant-condition' rule
    "no-constant-condition": "off",
  },
};
