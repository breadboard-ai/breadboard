[
  {
    "name": "execute_bash",
    "description": "Execute a bash command on the local server. The command runs in $HOME with stdout and stderr captured.",
    "parametersJsonSchema": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "The bash command to execute."
        },
        "timeout": {
          "type": "integer",
          "description": "Maximum seconds to wait. Default 30. Commands that exceed this are killed."
        }
      },
      "required": ["command"]
    },
    "responseJsonSchema": {
      "type": "object",
      "properties": {
        "stdout": {
          "type": "string",
          "description": "Combined stdout and stderr output."
        },
        "exit_code": {
          "type": "integer",
          "description": "Process exit code (0 = success)."
        },
        "error": {
          "type": "string",
          "description": "Error message if the command could not run."
        },
        "truncated": {
          "type": "boolean",
          "description": "True if output exceeded the 64 KB limit."
        }
      }
    }
  }
]
