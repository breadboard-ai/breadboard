{
  "name": "opal-backend-shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Opal backend logic (copybara-sharable)",
  "scripts": {
    "setup": "python3 -m venv .venv && PIP_INDEX_URL=https://pypi.org/simple/ .venv/bin/pip install -e '.[dev]'",
    "test": ".venv/bin/python -m pytest tests/ -v"
  }
}
