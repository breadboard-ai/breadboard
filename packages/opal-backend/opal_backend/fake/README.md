# `fake/` — Fake Backend Server

> **Not yet updated.** This code hasn't caught up with the rest of the migration
> and can be ignored for now.

The aspirational goal is a FastAPI server with canned scenarios for integration
testing — no real API calls, everything in-memory with hardcoded event
sequences. It would let the frontend team develop and test against predictable
agent behavior without needing live Gemini credentials.
