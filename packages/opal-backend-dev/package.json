{
  "name": "opal-backend-dev",
  "version": "0.1.0",
  "private": true,
  "description": "Opal dev backend (proxy existing APIs, wire new ones directly)",
  "scripts": {
    "setup": "python3 -m venv .venv && PIP_INDEX_URL=https://pypi.org/simple/ .venv/bin/pip install -e '../opal-backend-shared[dev]' && PIP_INDEX_URL=https://pypi.org/simple/ .venv/bin/pip install -e '.[dev]'",
    "dev": ".venv/bin/uvicorn opal_backend_dev.main:app --reload --port 8080"
  }
}
