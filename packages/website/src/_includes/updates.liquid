---
title: Breadboard | Updates
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Open Graph -->
    <meta name="og:title" content="{{title}} - Breadboard Updates" />
    {% if home_description %}<meta name="og:description" content="{{home_description}}" />{% endif %}
    {% if home_image %}<meta name="og:image" content="{{pkg.homepage}}{{home_image}}" />{% endif %}
    <meta name="og:url" content="{{pkg.homepage}}/breadboard{{page.url}}" />

    <!-- Twitter / X -->
    <meta name="twitter:title" content="{{title}} - Breadboard Updates" />
    {% if home_description %}<meta name="twitter:description" content="{{home_description}}" />{% endif %}
    {% if home_image %}<meta name="twitter:image" content="{{pkg.homepage}}{{home_image}}" />{% endif %}
    <meta name="twitter:card" content="summary_large_image" />

    <title>{{title}} | Updates | Breadboard</title>
    <link rel="icon" type="image/x-icon" href="/breadboard/static/icon.png" />
    <link rel="stylesheet" href="/breadboard/static/third_party/styles/prism-syntax-highlight.css" />
    <link rel="stylesheet" href="/breadboard/static/common.css" />
    <link rel="stylesheet" href="/breadboard/static/updates.css" />
  </head>
  <body>
  {% include "navigation.liquid" %}

  <aside class="updates">
    {% include "home.liquid" %}
    {% include "update-list.liquid" %}
  </aside>

  <main>
    {%- if not isIndex -%}
    <div><a class="back-to-main-list" href="/breadboard/updates/">Back to Updates</a></div>
    {%- endif -%}

    <h1>{{ title }}</h1>
    {{content}}
  </main>

  {% include "footer.liquid" %}
  </body>
</html>

