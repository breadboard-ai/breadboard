name: rendering
title: Testing Rendering Pipeline
description: A playbook to test a rendering pipeline.

steps:
  main:
    title: Rendering
    objective: >
      Create a super-fancy shopping list web app.
    skills: ["*"]
    model: gemini-3.1-pro-preview
    tags: ["ui", "bundle"]
    functions: ["system.*", "sandbox.*", "skills.*"]
