name: test
title: Test
description:
  Testing various capabilities of the system. Do not use this playbook for
  production.

steps:
  main:
    title: Test
    objective: >
      Write a short story about a robot and save it to a file. Return the file
      reference.
    tags: ["testing"]
    functions: ["system.*", "sandbox.*"]
